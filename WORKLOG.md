# 小源维护日志

## 2026-08-05（群聊语感自适应与上下文冒泡）

维护目标：在可靠性修复完成后继续降低“角色表演”和“标准答案”痕迹，让小源更像真正熟悉群聊节奏的人。

排查结论：
- 历史回复大量重复“喵 / 本猫 / 耳朵尾巴动作 / 技术比喻”，模型会从结构化历史继续模仿，即使新人设已经要求少用。
- 旧记忆库要求默认称呼“同学”，与当前群名片优先、自然接话的规则冲突；旧外号也缺少是否仍被本人接受的语境约束。
- 类人化回退策略把单独的“怎么 / 如何 / 为什么”当技术关键词，导致普通调侃和生活问题也进入 serious 风格。
- 分享近况、感谢问候、情绪求助和真正技术问题没有充分分开，容易出现“先复述、再建议、最后反问”的完整助理流程。
- 自动冒泡使用十条泛化问句，既没有点出真实话题，本地评分又把 bubble 和“新成员发言”一起扣分，实际上几乎永远无法发送。
- 最近机器人发过哪些短句没有单独记录，本地轻反应可能连续选中同一句。
- 高频 `/healthz`、`/readyz` 探测占满运行日志，不利于观察真实聊天；同时间批量写入的消息只按时间排序，记忆摘要边界存在偶发不确定性。

本次改动：
- 新增群聊语感分析：观察最近十二条群友消息的中位长度，动态提示碎片短句、普通短聊或完整表达，不复制原文、不模仿脏话。
- 新增抗重复提示与最终整理：检查最近六条真实机器人回复，近期已经反复使用猫系口癖、括号动作或硬塞技术比喻时，本轮主动禁用；同时清理重复称呼、舞台旁白和陈述后的泛化追问。
- 动态回复策略移到 system 层，当前成员消息保持为真正 user 内容；技术、实时信息和天气降低温度，闲聊与分享保留适度变化，不增加模型调用次数。
- 一轮回复只做一个主要动作。分享近况不主动追加教程、提醒或“下次记得”，感谢问候回半句收住，情绪词扩展到焦虑、失眠、委屈、压力等场景。
- 普通“怎么 / 为什么”不再自动判为技术求助；新增 `personal_share`、`social_ack` 和长口语疑问分类。
- 运行时记录最近实际发送文本并持久化，本地轻反应会避开刚发过的原句。
- 自动冒泡改为一次上下文生成：只有能点出课程、板子、报错、方案等具体未完对象时才生成 8-45 字跟进；“有人吗”“刚才那个后来咋样”等泛化话术直接丢弃。夜间、冷却和频率限制保持不变。
- 记忆库与人设统一为“线上 AI 顾问”，不默认补“同学”，旧外号只作识别参考；情绪惯性降为轻微语气延续，不再要求持续卖萌。
- 过滤守护进程健康探测访问日志；消息与摘要查询增加 ID 次级排序，压缩边界使用批次最小/最大 ID。

验证：
- `python -m pytest -q`：`120 passed`。
- `python -m ruff check src tests test_integration.py --ignore E402`：通过。
- `python -m compileall -q src bot.py test_integration.py`：通过。
- `python test_integration.py`：10 项集成烟测全部通过。
- 真实 MiMo 样本“我今天终于把环境装好了”最终回复为“不错，能跑了就好。”，无卖萌、无多余建议、无追问。
- 真实 MiMo 截图场景样本能明确说明“小源只在群里在线”，没有继续虚构自己在 6B504。
- 21:55 实机重启成功：OneBot V11 账号 `3115709797` 已连接，语义记忆 920 条就绪；连续健康探测未再写入业务日志。

## 2026-08-05（群聊上下文、可靠性与自然度全量审计）

维护目标：修复截图中的分段消息误解和现实身份幻觉，并系统处理群聊归因、发送可靠性、工具调用、响应速度、主动参与、跨平台启动和可维护性问题。

排查结论：
- 每条群消息在不同分支重复写入，显式回复、主动接话和普通环境消息看到的上下文并不一致；OneBot 事件重放还可能造成重复回复和画像重复计数。
- 用户先发“我怎么没在 6B504 看到猫娘”、随后单独 @ 机器人时，旧逻辑只看到空 @，既不会承接同一人的上一句，也缺少“机器人不在现实场所”的边界，因此会答非所问并虚构自己在窗台或机器后面。
- 引用消息主要依赖远端 `get_msg`，成员身份优先取 QQ 昵称而不是群名片；短时间多人发言时容易把话和人配错。
- 文本先写入助手记忆再发送，网络失败会留下“机器人已经说过”的假历史；发送成功后若记忆写入失败，又可能重试并让群里看到两遍。
- 记忆压缩会总结一批消息却删除超过该批次的旧消息；向量模型和向量读写占用事件循环，模型已缓存时仍会联网校验。
- 搜索和天气触发词过宽，“今天好累”“在外面吃饭”也可能误触发；天气位置写死成都，失败路径和超时不统一。
- 主动发言的近期状态只在内存里，重启后冷却、反馈和短期语境全部丢失；气泡消息会在缺少真实上下文时随机冒头。
- Windows/Linux 启动脚本的端口、Python 版本、依赖更新、QQ 桥接存活和退出清理还存在不一致，普通克隆默认分支也未包含此前功能分支的维护结果。

本次改动：
- 建立 OneBot 入站事件映射和数据库唯一约束，所有允许群消息先统一落库一次；私聊完全忽略，事件重放直接去重。
- 使用群名片作为首选身份，保存消息来源 ID、@ 对象和引用目标；引用优先本地精确解析，远端查询设短超时。
- 增加“同一成员 75 秒内先发正文、再单独 @”的安全承接，只拼接该成员自己的上一句，绝不借用其他群友的话。截图场景已做完整事件级回归测试。
- 重写人设边界：小源明确是在线 AI，不能声称自己真的位于 6B504、窗台或机器后面，也不能把猫耳等设定当现实事实；保留自然、短句、少量口癖，不再堆舞台动作。
- 新增统一发送事务：先确认 QQ 发送成功，再记录机器人发言、短期状态和助手记忆；记忆失败不重复发送，发送失败不制造假历史。
- 修复记忆压缩边界，向量写入和压缩改为有上限的后台任务；静默窗口增加单会话和全局队列上限及关停清理。
- 向量库后台降级启动，模型优先从本地缓存加载，确实缺失时才联网；健康接口可直接查看语义记忆状态。
- 搜索分类器只对明确检索命令或确实需要实时数据的问题联网；天气支持多城市、位置/结果缓存、Open-Meteo 与 wttr.in 兜底，并统一超时和诚实失败提示。
- 主动接话只把明确后续互动记为正反馈，无关下一句记为中性；重启后恢复群活跃时间、冷却、近期聊天、反馈和情绪状态。无真实上下文不发随机气泡，夜间静默继续硬拦截。
- 内部策略判断改用短提示和小 token 预算，明确 @ 或呼名走 0.8 秒快速窗口；搜索总超时 8 秒，最终回复 token 预算随字数上限调整。
- 新增 `/healthz` 和 `/readyz`，Windows/Linux 守护统一读取 HOST/PORT、检查 Python 3.11、按约束文件安装依赖、监控 Bot 与 QQ 桥接、归档日志并清理托管子进程。
- 固定直接依赖版本，补入运行时缺失的 `zhdate`；运行 PID 和启动历史不再进入版本库。

验证：
- `python -m pytest -q`：`106 passed`。
- `python -m ruff check src tests --ignore E402`：通过。
- `python -m compileall -q src bot.py test_integration.py`：通过。
- `python test_integration.py`：10 项集成烟测全部通过。
- PowerShell 解析及 MSYS2 Bash `-n` 语法检查通过。
- `pip install -e . -c constraints.txt --dry-run --quiet`：依赖解析通过。
- 21:19 实机重启成功：OneBot V11 账号 `3115709797` 已连接；`/healthz`、`/readyz` 均返回正常，语义记忆约 13 秒内从本地缓存就绪，载入 920 条向量记录。

## 2026-08-05

维护目标：严格排查“机器人在线时仍很少主动参与聊天”，修复真实参与率和开机可靠性。

排查结论：
- 开机启动日志显示机器人在 17:22 卡在向量模型初始化，启动脚本等待 120 秒后将其结束，因此 NoneBot 和 LLBot 都没有持续在线。
- LLBot 8 月 4 日原始群聊日志显示，19:05:37 的“机械制图居然不教 CAD”通过了主动机会筛选，但第一遍 AI gate 约耗时 19 秒；后续还有策略 AI、正式生成和 30 秒过期复检，最终未发送，却提前占用了 150 秒冷却。
- `poke_everyone_cooldown_minutes: 30` 会为许多群友的首次发言额外发起 AI 判断；现场“太牛了”触发的 `poke_general` 在 20 秒后超时，和文字聊天争用模型接口。
- 戳一戳被写入文字回复计数，会触发“机器人已经说过话”的后验取消；生成式主动接话排队后还会继续进入关键词反应，两个路径也可能互相取消。
- 主动回复在快节奏群聊里只允许 3 条新消息，实际模型生成期间很容易超过，导致适合接话的内容也被取消。

本次改动：
- 向量模型改为工作线程后台初始化，NoneBot 先完成端口监听和 OneBot 连接；语义记忆加载慢或失败不再阻塞基础聊天。
- Windows 启动器增加 Python/LLBot 崩溃自动重启、指数退避、历史日志归档和单实例互斥锁，避免开机/手动启动产生多个守护进程；冷启动保留已验证可用的 QQ 账号配置，不再每次删除重建。
- 生成式主动接话由正式模型在一次调用里完成“是否参与 + 回复内容”；不合适时返回内部 `[SILENT]`，去掉重复 AI 审批和额外策略等待。
- 冷却只在文字真正发送成功后开始；排队、AI 拒绝、超时和发送前取消都不再消耗冷却。
- 主动接话冷却调整为 90 秒，概率调整为 `react=0.55`、`short_reply=0.82`、`helpful_reply=0.95`；活跃群允许轻量接话，同时硬限制五分钟最多两次。
- 快节奏聊天的发送前取消阈值从 3 条放宽到 8 条，过期阈值从 30 秒放宽到 55 秒；新直接点名和机器人已经发言仍会取消旧回复。
- 关闭全员自动戳，保留指定成员和话题戳；话题戳优先使用 LLBot 的 `send_poke` API，并与文字回复计数分离。
- 主动接话排队后不再叠加关键词反应；新增 `[PROACTIVE]` 运行日志，记录评分、概率跳过、排队、AI 静默、后验取消和发送结果。
- 补充评分、五分钟硬上限、单次 AI 生成决策、冷却时机、后台初始化、启动自恢复、戳一戳 API 和默认配置测试。

验证：
- `python -m compileall -q src/plugins/xiaomo`
- `python -m pytest -q`
- 结果：`88 passed`
- PowerShell 语法解析通过，重复启动实测返回 `[Skip] Xiaomo bot supervisor is already running`。
- 真实启动约 1 秒完成端口监听，随后 OneBot V11 账号 `3115709797` 连接成功；向量库在后台完成写入。
- 群 `1070638552` 的真实消息“核异味”被评为 `react/44`，只调用一次正式模型，13 秒内生成 37 字主动回复并成功发送；随后显式 @ 回复也成功。

## 2026-08-03

维护目标：继续提升自然水群能力，加入除“分群性格配置”外的其他优化。

本次改动：
- 新增主动插话发送前后验检查：非 @ 主动回复在发送前会确认是否已经过时、群里是否已有多人接话、是否出现新的直接点名，避免话题过去了还硬发。
- 新增主动接话反馈学习：小源主动发言后，如果有人接着聊，会轻微提高后续主动概率；如果长时间没人接，会轻微降低。
- 新增本地轻反应：`react` 档命中“哈哈/寄了/有人吗/离谱/累”等短场景时，可直接发很短的本地反应，减少不必要 LLM 调用。
- 新增真人打字节奏：按回复长度、显式 @、主动插话和 `react` 动作计算短延迟；显式 @ 仍保持快速响应。
- 增强群友关系记忆：从允许群聊文本里提取兴趣方向和互动习惯，写入用户画像摘要；系统提示会看到“感兴趣方向/互动习惯”。
- 更新 README 中 `proactive_join.post_check`、`local_reactions_enabled` 和 `human_timing` 配置说明。
- 补充测试覆盖后验取消、反馈学习、轻反应、打字延迟、画像学习和默认配置。

验证：
- `python -m compileall -q src/plugins/xiaomo`
- `python -m pytest -q`
- 结果：`79 passed`

## 2026-08-03

维护目标：过一遍群聊参与链路，继续提升“像真人一样自然水群”的能力。

本次改动：
- 新增短期群聊文本流缓存 `state.group_recent_texts`，记录最近几句群聊作为运行时语境，不写入长期数据库，避免污染长期记忆。
- 主动接话前的 AI gate 现在会收到最近群聊流，能判断“这句插话接得上接不上”，减少突兀冒头。
- 主动接话真正进入 LLM 回复时，也会附带 `[RECENT_GROUP_FLOW]`，让回复能贴着刚刚几句话走。
- `proactive_join` 增加 `recent_context_messages` 配置，默认取最近 8 条实时群聊文本。
- 对不同接话动作增加硬长度预算：`react` 这类轻反应会按 `join_max_chars` 继续压短，避免轻插话变成小作文。
- 补充测试覆盖短期群聊流记录/过期、主动接话上下文块、接话预算、默认配置。

验证：
- `python -m compileall -q src/plugins/xiaomo`
- `python -m pytest -q`
- 结果：`69 passed`

## 2026-08-03

维护目标：让机器人进入 `1070638552` 群，并把非 @ 的自然水群参与度调高一些。

本次改动：
- `allowed_group_ids` 加入 `1070638552`，现在 `1056259135` 和 `1070638552` 都会被群聊事件处理。
- 将 `proactive_join` 冷却从 360 秒降到 150 秒，回复窗口从 1.2 秒降到 0.9 秒，提高接话时效性。
- 提高主动接话概率：`react=0.38`、`short_reply=0.68`、`helpful_reply=0.88`。
- 在 `interaction.py` 增加社交开口识别，如“有人”“聊聊”“水群”“有无”等，更容易自然接上闲聊。
- 降低普通疑问、情绪开口、观点开口的接话门槛，同时保留忙群、机器人刚连续发言后的硬刹车。
- 更新 README 中群白名单和主动接话配置说明。
- 新增配置默认值测试，并补充社交开口/轻情绪接话测试。

验证：
- `python -m compileall -q src/plugins/xiaomo`
- `python -m pytest -q`
- 结果：`64 passed`

## 2026-06-18

维护目标：提高群聊机器人可靠性、上下文理解能力、工具调用透明度和回复速度。

本次改动：

- 修复静默窗口批量消息按第一条消息归因的问题。现在会选择最合理的当前回复目标，并把同一窗口内其他群友的话作为带昵称的背景。
- 修复纯文本 `@` 误触发问题。只有明确 `@` 机器人 QQ 或配置昵称时才响应。
- 搜索工具新增结构化状态：区分已搜索、未触发、未配置、无可用结果。搜索失败时会要求模型诚实说明，不再假装查到了。
- 天气查询从收消息阶段移到回复处理阶段，并与搜索并发执行，减少 @ 后的等待。
- 记忆上下文不再重复注入最近聊天，当前消息也会从历史上下文中排除，避免模型把同一句话吃两遍。
- 群聊历史上下文批量加载群友昵称，减少“用户 QQ”导致的指代理解错误。
- 语义向量检索改用当前问题作为 query，并在压缩旧记忆后同步删除对应向量，减少旧记忆污染。
- 主动戳人先经过统一互动判断和 AI gate，避免夜间或不合适场景过度活跃。

验证：

- `python -m compileall -q src/plugins/xiaomo`
- `python -m pytest -q`
- 结果：`39 passed`

后续维护约定：

- 机器人显得“不聪明”时，先查三件事：当前发言人是否正确、工具状态是否被注入、记忆上下文是否重复或污染。
- 每次改动涉及行为策略、工具调用、记忆结构、主动互动，都要补至少一个回归测试。
- 重要维护继续记录在本文件，写清楚“为什么改、改了什么、怎么验证”。

## 2026-07-07

维护目标：继续提升群聊参与的类人化程度，让机器人先理解当前语境，再决定是否回复、是否查工具，以及回复多长。

本次改动：
- 新增本地群聊理解层 `intelligence.py`，在不额外调用 LLM 的情况下判断场景、语气、回复目标和工具计划。
- 将群聊理解结果接入 `handlers.py` 主回复流程，策略层和最终 LLM 都能看到“当前应该怎么接话”的隐藏提示。
- 收紧搜索触发：入口层传来的 `search_text` 只作为候选，只有明确搜索或明显实时问题才真正调用联网搜索；天气和普通闲聊不会误触发搜索。
- 天气、搜索、图片、记忆等工具意图统一进入 `ToolPlan`，避免一个问题同时被多个旧规则抢着解释。
- 增加最终回复自检：去掉可能泄漏的系统提示前缀，按场景二次限制长度，让闲聊回复更像群友顺手接一句。
- 新增 `tests/test_social_intelligence.py`，覆盖天气优先、普通 @ 不搜索、明确搜索、实时问题搜索、闲聊短回复和提示泄漏清理。

验证：
- `python -m compileall -q src/plugins/xiaomo`
- `python -m pytest -q`
- 结果：`45 passed`
- 重启验证：NoneBot 进程已启动，uvicorn 监听 `127.0.0.1:8080`，OneBot V11 显示 `Bot 3115709797 connected`。

## 2026-07-07

维护目标：让机器人更积极地自然加入群聊，但仍然避免刷屏、抢话和夜间打扰。

本次改动：
- 在 `interaction.py` 新增主动插话机会评分器，输出 `silent`、`react`、`short_reply`、`helpful_reply` 四档动作。
- 评分综合求助意图、疑问句、情绪开口、有趣话题、群热闹程度、机器人近 5 分钟发言次数、距离上次机器人发言时间，以及机器人发言后是否已有足够人类轮次。
- 在 `handlers.py` 接入非 @ 群聊消息：高分机会先过本地评分、群级冷却、概率门和原有 AI gate，通过后才排入正常 LLM 回复队列。
- 移除旧的 5% 固定话术接话逻辑，避免脚本感和双重主动发言。
- 新增 `proactive_join` 配置：默认 10 分钟群级冷却，按动作设置触发概率，主动插话窗口为 1.5 秒。
- 新增测试覆盖高分求助、热闹/刚发言时沉默、夜间沉默、决策 payload 稳定性。

验证：
- `python -m compileall -q src/plugins/xiaomo`
- `python -m pytest -q`
- 结果：`49 passed`
- 重启验证：NoneBot PID `21272` 已启动，uvicorn 监听 `127.0.0.1:8080`，OneBot V11 显示 `Bot 3115709797 connected`。

## 2026-07-07

维护目标：让最终回复少一点“AI 答案味”，更像群友自然说话，同时不改事实内容。

本次改动：
- 新增 `tone_polisher.py`，提供发送前本地语气修整：去掉正式答题壳、压短闲聊、减少重复口癖、清理空 markdown 外壳和隐藏提示泄漏。
- 新增 `build_tone_instruction`，在 LLM 生成前提示它先接语境、少写作文式开头、不要自我介绍或强行卖萌。
- 在 `handlers.py` 最终回复链路中接入：`post_check_reply` → `shape_reply` → `polish_tone`，保留原有可靠性检查，再做语气自然化。
- 对代码块和表格做保护，避免语气修整破坏技术回答里的代码内容。
- 新增 `tests/test_tone_polisher.py`，覆盖正式开头清理、闲聊列表压短、代码块保留、口癖去重和语气提示预算。

验证：
- `python -m compileall -q src/plugins/xiaomo`
- `python -m pytest -q`
- 结果：`53 passed`
- 重启验证：NoneBot PID `30476` 已启动，uvicorn 监听 `127.0.0.1:8080`，OneBot V11 显示 `Bot 3115709797 connected`。

## 2026-07-07

维护目标：把主动插话调得更积极，同时保留防刷屏刹车。

本次改动：
- 下调主动插话动作阈值：普通疑问句更容易进入 `short_reply`，求助类更容易进入 `helpful_reply`。
- 提高基础插话分和“冷场/低消息量”加分，轻微降低中等活跃群聊和较久之前机器人发言的扣分。
- 保留强约束：夜间不主动、刚说过话强扣分、机器人 5 分钟内说太多仍强扣分、极热闹群仍降权。
- 调整 `proactive_join` 默认配置：群级冷却从 600 秒降到 360 秒，窗口从 1.5 秒调到 1.2 秒，触发概率提升为 `react=0.22`、`short_reply=0.50`、`helpful_reply=0.76`。
- 新增测试覆盖中等疑问句会进入短接话、观点开口至少能触发轻反应。

验证：
- `python -m compileall -q src/plugins/xiaomo`
- `python -m pytest -q`
- 结果：`55 passed`
- 重启验证：NoneBot PID `47180` 已启动，uvicorn 监听 `127.0.0.1:8080`，OneBot V11 显示 `Bot 3115709797 connected`。

## 2026-07-20

维护目标：支持 Linux 端启动水群功能，并修复开机自启动不稳定的问题。

本次改动：
- 新增 `start_bot.sh`，Linux/macOS 可直接启动 NoneBot；会创建日志目录、检查 `.env`/人设文件、等待 8080 就绪，并在找到 Linux LLBot 可执行文件时自动拉起桥接。
- 新增 `scripts/install_linux_autostart.sh`，安装 systemd user service：`xiaomo-bot.service`，支持 `--now` 立即启动、`--no-llbot` 外部桥接模式、`--disable` 移除服务。
- 新增 `scripts/install_windows_autostart.ps1`，用 Startup 快捷方式安装 Windows 开机自启动，比直接放 bat/vbs 更稳。
- 修复 `start_bot.vbs`：改为绝对路径加引号启动 `start_bot.bat`，避免开机启动时工作目录不对或中文路径被 cmd 误解析。
- 修复 `start_bot.bat`：增加 `-NoProfile`，并显式使用脚本目录。
- 修复 `start_bot.ps1`：不再全局杀掉所有 `node`/`llbot`/`pmhq`；只清理本项目相关进程。若 8080 被无关进程占用，会报错退出而不是误杀。
- 移除自启动场景下的 `Read-Host` 交互等待；缺 `.env` 或依赖失败时直接写日志并退出，避免开机后台卡住。
- 更新 `README.md`，补充 Linux 启动、Linux systemd 自启动、Windows 自启动安装/移除命令。
- 新增 `tests/test_startup_scripts.py`，覆盖 Linux/Windows 启动资产和关键防回退规则。

验证：
- `python -m compileall -q src/plugins/xiaomo`
- `python -m pytest -q`
- 结果：`60 passed`
- PowerShell 静态语法检查：`start_bot.ps1`、`scripts/install_windows_autostart.ps1` 均通过。
- Linux bash 实机语法检查未在本机完成：当前 Windows 环境的 WSL 虚拟化不可用，因此用文本级脚本测试兜底。
- 当前 Windows 用户自启动已更新：`C:\Users\zhr\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\Xiaomo QQ Bot.lnk` 指向新的 `start_bot.vbs`。
